# 03 - Traceability Requirements

Every commit, PR, and code comment must reference the corresponding TASK-XXXX.

Example commit message:
`feat(retention): implement win-back action executor (TASK-LR-03)`

Never submit code without a linked task.