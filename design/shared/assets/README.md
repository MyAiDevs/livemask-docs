# Shared Assets

Put shared exported assets here only when they are used by more than one surface.

Examples:

```text
logo/
icons/
illustrations/
product-mockups/
```

Assets used by only one surface should stay under that surface's design folder
until they are moved into the implementation repo.
