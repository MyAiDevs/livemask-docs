# LiveMask App Atoms Export v2

Raw export:

```text
design/app/atoms/v2/export/
```

Source folder moved here:

```text
LiveMask App Design Brief
```

This export contains cross-platform App UI code and files for:

- iOS
- Android
- macOS
- Windows
- Linux

App developers should treat this as design source material, not as runtime code
to copy wholesale. Implementation still belongs in `livemask-app`.
