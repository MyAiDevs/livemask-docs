# LiveMask Admin Design

This directory stores Admin console design artifacts.

Primary design brief:

```text
docs/admin/LIVEMASK_FRONTEND_DESIGN_BRIEF_FOR_ATOMS.md
```

Current design version:

```text
design/admin/atoms/v1
```

Current combined Atoms export for system admin, sponsor ambassador, and
promotion ambassador UI:

```text
design/frontend-suite/atoms/v1/export/
```

This combined export is shared with the public website design because the Atom
output contains login, registration, sponsor/ambassador, promotion ambassador,
and system administrator surfaces in one generated project.
